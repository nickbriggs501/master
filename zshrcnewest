if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

# If you come from bash you might have to change your $PATH.
# export PATH=$HOME/bin:/usr/local/bin:$PATH
# Path to your oh-my-zsh installation.
export ZSH=$HOME/.oh-my-zsh
# See https://github.com/ohmyzsh/ohmyzsh/wiki/Themes

ZSH_THEME="powerlevel10k/powerlevel10k"
#Favorites are powerlevel10k/powerlevel10k af-magic simple agnoster
#Ones I like. simple agnoster robbyrussell powetlevel10k/powerlevel10k risto nanotech mrtazz gentoo gallois cypher candy bira awesomepands amuse avit alanpeabody robbyrussell af-magic))
# ZSH_THEME_RANDOM_CANDIDATES=( "a" "b" )
# CASE_SENSITIVE="true"
# Case-sensitive must be off for hyphen
# HYPHEN_INSENSITIVE="true"
# DISABLE_AUTO_UPDATE="true"
# DISABLE_UPDATE_PROMPT="true"
# export UPDATE_ZSH_DAYS=13
# DISABLE_MAGIC_FUNCTIONS=true (fix links)
# DISABLE_LS_COLORS="true"
# DISABLE_AUTO_TITLE="true"
# ENABLE_CORRECTION="true"
COMPLETION_WAITING_DOTS="true"
# DISABLE_UNTRACKED_FILES_DIRTY="true"
# "mm/dd/yyyy"|"dd.mm.yyyy"|"yyyy-mm-dd"
# see 'man strftime' for details.
# HIST_STAMPS="mm/dd/yyyy"
# ZSH_CUSTOM=/path/to/new-custom-folder
# plugins found in ~/.oh-my-zsh/plugins/*
# Custom plugins add to ~/.oh-my-zsh/custom/plugins/
plugins=(git)
source $ZSH/oh-my-zsh.sh
# export MANPATH="/usr/local/man:$MANPATH"
# export LANG=en_US.UTF-8

# if [[ -n $SSH_CONNECTION ]]; then
#   export EDITOR='vim'
# else
#   export EDITOR='mvim'
# fi

# Compilation flags
# export ARCHFLAGS="-arch x86_64"

#ALIAS
alias mka="nano +56 -c $HOME/.zshrc"
alias b="bash"
alias n="nano -c"
alias x="exit"
alias c="chmod +x"
alias m="mkdir"
alias cl="clear"
alias r="rm -r"
alias R="rm -rf"
alias l="ls -a"
alias L="ls -al"
alias p="python"
alias p2="python2"
alias sd="cd $HOME/storage/dcim"
alias cds="cd $HOME/scripts"
alias cdp="cd $HOME/projects"
alias cdss="cd $HOME/.scripts"
alias T="cd ~/.oh-my-zsh/themes"
alias t="nano +11,39 -c $HOME/.zshrc"
alias S="cp -r $HOME/scripts $HOME/storage/dcim"
alias P="cp -r $HOME/projects $HOME/storage/dcim"
alias .s="cp -r $HOME/.shortcuts $HOME/storage/dcim"
alias .z="cp $HOME/.zshrc $HOME/storage/dcim"
alias .d="bash $HOME/scripts/dragon.sh"
alias color="bash $HOME/scripts/colors.sh"
alias .list="nano /etc/apt/sources.list"
alias cow="cowsay"
alias C="cowsay -l"
alias f="figlet"
alias toy="toilet"
alias ss="cmatrix -s"
alias fire="cacafire"
alias msf="msfconsole"

source /data/data/com.termux/files/home/.zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
#Optionally to hide the “user@hostname” info when you’re logged in as yourself on your local machine, add prompt_context(){}
# To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh

